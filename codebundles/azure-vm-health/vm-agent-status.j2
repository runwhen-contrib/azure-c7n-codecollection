policies:
  - name: vm-agent-status
    resource: azure.vm
    subscription: {{subscriptionId}}
    filters:
      # Check for VMs where the agent is not ready
      - type: instance-view
        key: vmAgent.statuses[0].code
        op: ne
        value: "ProvisioningState/succeeded"
      - type: value
        key: resourceGroup
        op: eq
        value: {{resourceGroup|upper}}